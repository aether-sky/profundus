package sky.aether

import com.deweyvm.gleany.{GleanyGame, GleanyInitializer}

class Game(initializer:GleanyInitializer) extends GleanyGame(initializer) {
  //val sprite =
  override def draw(): Unit = ()
  override def update(): Unit = ()
}
